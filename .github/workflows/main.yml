name: Build Partisan Telegram Standalone APK

on:
  push:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Create Keystore for Release
      run: |
        mkdir -p ~/.android
        keytool -genkey -v -keystore ~/.android/debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

    - name: Build Full Project Release APK
      run: ./gradlew assembleAfatRelease -x test --continue --stacktrace || ./gradlew assembleRelease -x test --continue --stacktrace

    - name: Find and Copy Real APK
      run: |
        mkdir -p final_apk_only
        find . -type f -name "*.apk" -exec cp {} final_apk_only/ \;
        echo "=== GENERATED APK FILES ==="
        ls -la final_apk_only/

    - name: Upload Final APK
      uses: actions/upload-artifact@v4
      with:
        name: Partisan-Telegram-APK
        path: final_apk_only/
        
